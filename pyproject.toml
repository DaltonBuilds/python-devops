[project]
name = "python-devops"
version = "0.1.0"
description = "Python learning for DevOps engineers"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
    "jupyterlab>=4.5.5",
]
